@model IEnumerable<Book_Shoppe.Entity.Language>
@{
    ViewBag.Title = "ManageBookLanguage";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Manage Book Language</h2>

@if (ViewBag.Message != null)
{
    <div class="alert alert-dismissible alert-primary">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <strong>@ViewBag.Message</strong>
    </div>
}
@if (ViewBag.Alert != null)
{
    <div class="alert alert-dismissible alert-warning">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <strong>@ViewBag.Alert</strong>
    </div>
}
<table class="table table-hover">
    <thead>
        <tr class="table-primary">
            <th>Sno</th>
            <th>Genre Name</th>
            <th>Operations</th>
        </tr>
    </thead>
    <tbody>
        @{
            int count = 1;
            foreach (var item in Model)
            {
                <tr class="table-light">
                    <td>@count</td>
                    <td>@item.LanguageName</td>
                    <td class="btn-group" role="group" aria-label="Basic example"> @Html.ActionLink("Delete", "DeleteLanguage", new { id = item.LanguageID }, new { @class = "btn btn-secondary", onclick = "return confirm('Are you sure, you want delete this Language?');" })</td>
                </tr>
                count++;
            }
        }
    </tbody>
</table>
<br />

@using (Html.BeginForm("AddLanguage", "Book", FormMethod.Post, new { @class = "form" }))
{
    @Html.AntiForgeryToken()
    <label class="col-form-label">Add New Language</label>
    <input class="form-control" type="text" name="LanguageName" />
    <br />
    <input type="submit" class="btn btn-outline-primary" id="submit" />
}

